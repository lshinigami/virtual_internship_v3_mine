**Контекст задачи (краткое описание) **
Написать тест на Junit

**Что было сделано и как? (краткое описание)**
Долго гуглила как вводить формать Date
из-за него было много изменений основного кода в класса request response
в итоге перешла на класс LocalDate и везде вводила и возвращала в формате CharSequence

В написании теста не было нчего трудного, но нужно учесть, что равенство опеределяется через класс Assertion.assertEquals()
Для запуска теста используется import org.junit.jupiter.api.Test;

**Зачем это было сделано? Чему научился в процессе выполнения задания?**
я научилась писать не сложные тесты

**Сколько потратил времени на выполнение задачи?**
Ровно 1 час

**Заметки, идеи, предложения в свободной форме**!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
Assertion.assertEquals()
Для запуска теста используется import org.junit.jupiter.api.Test;